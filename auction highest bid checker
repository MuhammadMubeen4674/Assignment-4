#include<iostream> 
using namespace std; 
int main() { 
int highestBid = 0, bid; 
cout << "Enter 5 bids:\n"; 
for (int i = 0; i < 5; i++) { 
cin >> bid; 
if (bid > highestBid) { 
highestBid = bid; 
} 
} 
cout << "\nThe highest bid is: " << highestBid << endl; 
return 0; 
} 
